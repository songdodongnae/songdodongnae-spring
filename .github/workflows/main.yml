# name: CI/CD Pipeline

# on:
#   push:
#     branches:
#       - develop

# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2
    
#       - name: Login to Docker Hub
#         uses: docker/login-action@v1
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
          
#       - name: Build and push Docker image
#         uses: docker/build-push-action@v2
#         with:
#           context: .
#           push: true
#           tags: suee97/spring:deploy
    
#       - name: Deploy to server
#         # SSH를 통해 Amazon Linux 서버에 접속하여 Docker 명령어를 실행합니다.
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.SSH_HOST }}
#           username: ${{ secrets.SSH_USERNAME }}
#           key: ${{ secrets.SSH_KEY }}
#           script: |
#             echo "SSH 연결 성공! 서버에 배포 시작"
            
#             # SSH 키 Secret이 제대로 전달되었는지 확인합니다.
#             # 이 키는 appleboy/ssh-action에 의해 별도의 파일로 처리됩니다.
#             # 따라서 키 값의 일부가 아닌, 키의 존재 여부만 확인하는 것이 안전합니다.
#             if [ -n "${{ secrets.SSH_KEY }}" ]; then
#               echo "SSH 키 Secret이 성공적으로 전달되었습니다."
#               echo "전달된 SSH_KEY Secret 길이: ${#secrets.SSH_KEY}"
#             else
#               echo "SSH 키 Secret이 비어있거나 전달되지 않았습니다."
#             fi
    
#             # 기존 컨테이너 중지 및 삭제 (오류 무시)
#             # Amazon Linux에서도 동일한 Docker 명령어가 사용됩니다.
#             if docker ps -a -q -f name=spring | grep .; then
#               echo "기존 'spring' 컨테이너를 중지하고 삭제합니다."
#               docker stop spring
#               docker rm spring
#             fi
    
#             # 최신 Docker 이미지 pull
#             echo "최신 Docker 이미지를 pull합니다."
#             docker pull suee97/spring:deploy
    
#             # 새로운 Docker 컨테이너 실행
#             echo "새로운 'spring' 컨테이너를 실행합니다."
#             docker run -d \
#               --name spring \
#               -p 8080:8080 \
#               -e MYSQL_HOST=${{ secrets.MYSQL_HOST }} \
#               -e MYSQL_PORT=${{ secrets.MYSQL_PORT }} \
#               -e MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }} \
#               -e DATABASE_USERNAME=${{ secrets.DATABASE_USERNAME }} \
#               -e DATABASE_PASSWORD='${{ secrets.DATABASE_PASSWORD }}' \
#               -e spring.datasource.url="jdbc:mysql://${{ secrets.MYSQL_HOST }}:${{ secrets.MYSQL_PORT }}/${{ secrets.MYSQL_DATABASE }}" \
#               -e spring.datasource.username=${{ secrets.DATABASE_USERNAME }} \
#               -e spring.datasource.password=${{ secrets.DATABASE_PASSWORD }} \
#               -e S3_ACCESS_KEY=${{ secrets.S3_ACCESS_KEY }} \
#               -e S3_SECRET_KEY=${{ secrets.S3_SECRET_KEY }} \
#               -e JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }} \
#               -e NAVER_CLIENT_ID=${{ secrets.NAVER_CLIENT_ID }} \
#               -e NAVER_SECRET=${{ secrets.NAVER_SECRET }} \
#               -e KAKAO_CLIENT_ID=${{ secrets.KAKAO_CLIENT_ID }} \
#               -e GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }} \
#               -e GOOGLE_SECRET=${{ secrets.GOOGLE_SECRET }} \
#               -e KAFKA_IP=${{ secrets.KAFKA_IP }} \
#               suee97/spring:deploy
            
#             echo "배포가 성공적으로 완료되었습니다!"
